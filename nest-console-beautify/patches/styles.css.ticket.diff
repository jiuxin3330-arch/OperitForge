--- src/styles.css.bak-ticket-1787597453	2026-08-24 14:16:13.477316381 +0800
+++ src/styles.css	2026-08-25 02:51:31.185287152 +0800
@@ -14877,25 +14877,37 @@
 }

 .nest-proposal {
+  position: relative;
   background: linear-gradient(180deg, var(--nest-ticket-hi), var(--nest-ticket));
-  border-radius: 14px;
-  padding: 14px 14px 12px;
+  border-radius: 16px;
+  padding: 15px 16px 12px;
   color: var(--nest-text);
-  box-shadow: 0 1px 2px rgb(39 44 40 / 0.06);
-  margin-bottom: 10px;
+  box-shadow: 2px 3px 10px rgb(120 96 52 / .1);
+  margin-bottom: 12px;
 }
+html[data-theme="dark"] .nest-proposal { box-shadow: 0 1px 2px rgb(0 0 0 / 0.25); }
 .nest-proposal-tag {
+  display: flex;
+  align-items: center;
+  gap: 8px;
   font-family: 'IBM Plex Mono', ui-monospace, monospace;
   font-size: 9.5px;
   color: var(--nest-text);
   opacity: 0.65;
-  letter-spacing: 0.05em;
-  margin-bottom: 4px;
+  letter-spacing: 0.08em;
+  margin-bottom: 6px;
 }
-.nest-proposal-name { font-size: 14px; font-weight: 500; margin-bottom: 2px; }
+.nest-proposal-tag::after {
+  content: '';
+  flex: 1;
+  height: 1px;
+  background: var(--nest-line);
+}
+.nest-proposal-name { font-size: 14.5px; font-weight: 500; margin-bottom: 2px; }
 .nest-proposal-code {
   font-family: 'IBM Plex Mono', ui-monospace, monospace;
   font-size: 10px;
+  letter-spacing: 0.05em;
   color: var(--nest-text);
   opacity: 0.55;
   display: block;
@@ -14911,19 +14923,41 @@
   opacity: 0.9;
 }
 .nest-proposal-quote {
-  margin: 0 0 10px;
+  margin: 0 0 12px;
   font-size: 11.5px;
   font-style: italic;
   color: var(--nest-text);
-  opacity: 0.75;
-  border-left: 2px solid rgb(39 44 40 / 0.15);
+  opacity: 0.8;
+  border-left: 2px solid color-mix(in srgb, var(--nest-butterfly) 45%, transparent);
   padding-left: 8px;
 }
+/* 票根撕線:虛線 + 兩側打孔。圓孔填 --nest-bg(與 .workspace 底色同源),兩個主題自動貼合 */
 .nest-proposal-actions {
+  position: relative;
   display: flex;
   gap: 8px;
   justify-content: flex-end;
+  margin: 2px -16px 0;
+  padding: 11px 16px 0;
+  border-top: 1.5px dashed var(--nest-line);
+}
+.nest-proposal-actions::before,
+.nest-proposal-actions::after {
+  content: '';
+  position: absolute;
+  top: -8px;
+  width: 15px;
+  height: 15px;
+  border-radius: 50%;
+  background: var(--nest-bg);
+  box-shadow: inset 1px 1px 2px rgb(91 79 60 / .12);
+}
+html[data-theme="dark"] .nest-proposal-actions::before,
+html[data-theme="dark"] .nest-proposal-actions::after {
+  box-shadow: inset 1px 1px 2px rgb(0 0 0 / .35);
 }
+.nest-proposal-actions::before { left: -8px; }
+.nest-proposal-actions::after { right: -8px; }

 .nest-btn {
   padding: 8px 16px;

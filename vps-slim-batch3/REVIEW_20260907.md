# 第三批覆核(規劃窗,2026-09-07 00:25)——通過,無需返工;一條小補件

對照 `NOTES_VPS_SLIM_BATCH3_20260906.md`,親手在 VPS 驗,不是讀報告。

## 逐項

| 項 | 驗法 | 結果 |
|---|---|---|
| ① 服務 | `is-active`/`is-enabled` 逐一 | mumu-panel/mumu-chat/multipathd(+socket)/ModemManager inactive+disabled;fwupd/udisks2/upower inactive+masked,mask 後 1 小時 dbus 沒再拉起來 |
| ① 沒刪檔 | `ls` unit 檔與程式目錄 | `/etc/systemd/system/{mumu-panel,mumu-chat,memory-dashboard}.service`、`/root/mumu-server/panel.py`、`/root/mumu-chat/server.py`、`/root/memory-dashboard/`(1.6G)全在 |
| ① ufw | `ufw status` | 22/8000/8003(v4+v6);3000/8080/8888 已不在 |
| ② 先破後立 | `review_e2e_mumu.sh`(systemd-run 獨立 cgroup) | 停 `.path` → 派 job:觸發檔**有立**、job **卡 queued**、worker inactive;開 `.path` → 1 秒 active、觸發檔收走、**4 秒 completed**;閒置 61 秒 exit 0;`/proc/*/comm` chrome=0 node=0;trap 保證 `.path` 復原 |
| ② 生產檔 | md5 vs repo | 兩支腳本 + 六個 unit 檔逐位元相同;main.py 有 `_signal_screenshot_worker`,worker.mjs 有 `idleExitMs`,conftest 有 `CHATNEST_SCREENSHOT_TRIGGER` 指 tmp |
| ② 測試不擾生產 | 跑修過的 7 支測試檔,同時看 worker journal 與觸發目錄 | 54 passed;測試期間生產 worker 0 次啟動、觸發目錄空 |
| ④ .bak | 數檔 | `backups/app-bak-20260906/` 10 個;原地 3 個回滾點 |
| 一週觀察 | `ls -l` + `list-timers` | `swap_watch.csv` 屬 nestmemory,00:00 準時多一列;dispatch 安全網 00:00:57 跑過(queued=0) |
| ⑤ | `is-active chatnest.service` | active,未動 |

## 小補件(不急,下次順手)

`_signal_screenshot_worker` 沒有自己的測試。加一條:派 job 後 `CHATNEST_SCREENSHOT_TRIGGER`
指到的檔要存在;拿掉 `_signal_screenshot_worker()` 那行呼叫要紅。

## 觀察到、不歸這批

- 00:00 那列 `bridge_mb=424`(23:50 是 225)——bridge 在對話時會長很大。一週曲線看的時候記得這欄,
  若 swap 又爬回去,元凶可能在 bridge 不在被停掉的那些。
- 記憶體用量 23:50 的 635MB 到 00:11 已回到 944MB,單點數字不能當結論,照 NOTES 說的看 csv。

## ③ 裁定

交糯糯(甲/乙/丙)。規劃窗意見同工作窗:甲。4GB 決定等 9/13 看 csv。

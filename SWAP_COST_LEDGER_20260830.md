# Swap 實驗第一步:成本總帳(補刀 A)

2026-08-30|資料來源:`turn_usage` + `actor_thread_history`(app.sqlite3,唯讀查詢,未寫任何代碼)
範圍:actor=mumu,2026-08-06 ~ 2026-08-29(turn_usage 全量 491 turns,其中 mumu 487)

---

## 一、現況實測

### compact 實際頻率
- **7 次 / 16.4 天 = 0.43 次/天(約 1 次 / 2.3 天)**(2026-08-13 ~ 08-29)
- 忙日會連發:08-16 一天 3 次(當天 69 turns)

### compact 觸發點與基線(逐次實測)

| compact 時間 | 觸發時 context | % | 後基線 |
|---|---|---|---|
| 08-13 06:31 | 196,881 | 98 | 37,515 |
| 08-16 08:02 | 180,930 | 90 | 48,316 |
| 08-16 16:14 | 190,207 | 95 | 34,196 |
| 08-16 23:31 | 172,850 | 86 | 35,768 |
| 08-19 15:32 | 197,209 | 99 | 45,603 |
| 08-24 06:38 | 199,905 | **100** | 52,041 |
| 08-29 15:32 | 199,124 | **100** | 55,492 |

- 名義 `auto_compact_threshold` = 167,000(83.5%),但**實際觸發全部在 86%~100%,平均 ~191k**;
  最近兩次直接頂到 100% 硬上限——現行 compact 本身就經常是「撞牆才觸發」,
  這正是規格點 3(margin 由程式算)要防的事,現況數據直接佐證。
- compact 後基線 34k~55k,**且趨勢上升**(最近三次 45.6k / 52.0k / 55.5k)——
  系統提示+常駐 context 在長大,做 Swap 預算時基線不能當常數。

### context 成長率(同 session 逐 turn delta,435 樣本)
- 中位 1,363|平均 **2,892**|p90 6,681|p95 8,305|**max 23,455** tokens/turn

### 對話節奏與總開銷(近 14 天)
- **23.9 turns/天**(日波動 5~69)
- 每日開銷:cache_creation ~2.28M、cache_read ~3.62M、output ~16.2k、uncached ≈ 0

### 模型自洽性驗證
用實測參數反推 compact 頻率:實際容量 =(平均觸發 191k − 平均基線 44k)= 147k,
每循環 147k ÷ 2,892 = 50.8 turns → 23.9 ÷ 50.8 = **推算 0.47 次/天 vs 實測 0.43 次/天**。
模型與現實吻合,以下投影可信。

---

## 二、Swap 預計頻率 = f(觸發點)

參數:
- **重生包 ~60k**(規格數字;對照實測 compact 後基線 44k + 近段原文/Tool Primer 增量,量級合理)
- **margin 實測依據**:單 turn 最壞成長 23.5k、p95 8.3k → margin 30k(=max+緩衝)是有數據的保守值,20k 偏險(一個 max turn 就吃掉)
- 頻率 = 每日 context 成長(23.9 × 2,892 ≈ 69.1k/天)÷ 每循環容量(觸發點 − 60k)

| Swap 觸發點 | margin 解讀 | 每循環容量 | turns/循環 | **次/天** | **注入成本/天(×60k)** |
|---|---|---|---|---|---|
| 137k | 167k 名義閾值 − 30k | 77k | 26.6 | **0.90** | 54k |
| 160k | — | 100k | 34.6 | **0.69** | 41k |
| **170k** | **200k 硬上限 − 30k(建議)** | 110k | 38.0 | **0.63** | **38k** |
| 180k | 200k − 20k(偏險) | 120k | 41.5 | 0.58 | 35k |

- 對照 compact 實測 0.43 次/天:建議檔(170k)下 **Swap 頻率約為 compact 的 1.5 倍**(1 次/1.6 天)。
- 忙日(69 turns ≈ 200k 成長)單日可能 Swap ~1.8 次;閒日(5~6 turns)可數日不 Swap。

## 三、成本結論

- **Swap 新增開銷 ≈ 38k cache_creation/天(建議檔),佔目前每日 cache_creation(2.28M)的 ~1.7%。可忽略。**
- 且 compact 不是免費對照組:compact 後首 turn 的 cache 重建實測 8k~362k(7 次平均 ~147k),
  與 Swap 的 60k 注入同量級甚至更貴——Swap 相對 compact 的**淨增成本可能接近零或為負**。
- 敏感度:若重生包漲到 70k(基線上升趨勢),建議檔頻率 0.69 次/天、成本 48k/天,結論不變。
- 未計入項(第三步「離場結算」另帳):Swap 前即時抽取的 extractor 呼叫、resume 後驗證 ping。
  均為 Haiku/小額呼叫,不影響上表量級。

## 四、給規劃窗的裁定建議

1. **數據支持進第二步(Swap MVP)**:頻率 1.5×、成本佔比 1.7%,「頻率×60k」這筆帳過關。
2. Swap 觸發點建議掛在**硬上限 − margin(200k − 30k = 170k)**,不掛名義 167k 閾值——
   實測顯示現行系統本來就跑到 86~100% 才 compact,掛 137k 會把頻率推到 0.90 次/天,白繳 40% 成本。
3. margin 30k 有實測依據(單 turn max 23.5k),寫死進程式判定(規格點 3)。
4. 重生包預算要留浮動:基線一個月內從 34k 爬到 55k,manifest(規格點 6)應記錄每次重生包實際大小,漂移可追。

*查詢腳本:/tmp/swap_cost_probe{,2,3}.py(VPS,唯讀)*

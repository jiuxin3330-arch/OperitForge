# 交接｜工作窗 2026-09-02

分支 `claude/hotfix-backport-security-exposure-sfghdw`,共 7 個 commit。
底下每一節都對應 repo 裡一份更詳細的 NOTES,這裡只寫**下一個窗口需要知道的事**。

---

## 一句話狀態

| 工單 | 狀態 |
|---|---|
| 第一批① 9/1 兩個熱修回寫 | ✅ 完成,規劃窗已 VERIFIED |
| 第一批② 對外暴露 | ✅ 完成(方案 A 廢案 → 方案 B 全數上線,舊路徑已 404) |
| TICKET-H StackChan 照片進相簿 | ✅ 施工項 ①②③⑤ 完成並經糯糯驗收;**④ 舊面板下線待裁定** |

---

## 一、9/1 熱修回寫(`hotfix-20260901/`)

兩支生產檔案原本只存在 VPS,下次部署會被覆蓋。已 md5 逐位元比對回寫,附 11 條迴歸測試。

- `autonomy_runner.py`:`dnd_minutes = min(DND_MINUTES, slot["interval_minutes"])`
- `swap_runner.py`:turn_usage 讀數去重(`usage_created_at` + `swap_last_consumed.json`)

測試用 `HOTFIX_PROD_DIR` 切換受測對象,**同一份測試可以打生產檔也可以打修復前的備份**——
拆掉修復會紅 5 條,這是雙向驗證,不是只有綠燈。

**掃描結論**:其他排程沒有同型迴圈。swap_runner 是唯一一支,結構性原因是
「它讀的訊號由第三方產生,而它自己的動作推不動那個訊號」。這個形狀值得當常設檢查項。

> ⚠️ **待觀察**:`swap_last_consumed.json` 到 9/2 為止還沒被產生過,
> 表示去重邏輯**尚未在生產跑過一次**。下次換窗後請確認這個檔案有出現。

## 二、對外暴露(`security/`)

方案 A(Cloudflare Access)實測廢案:VPS 上唯一那把憑證對 Zero Trust 只有讀權限。
改行方案 B(密鑰路徑 `/mcp-<KEY>`),照鐵律五步雙路並存過渡,四支服務(voice /
stackchan / anchor / hands)全數完成,舊 `/mcp` 現在 404。

- 過渡層 `mcp_path_alias.py` 四支共用同一份(md5 一致),14 條規則測試鎖住尾斜線、
  前綴黏字、`../` 三種繞道
- 密鑰的**唯一正本**在 `/srv/chatnest-next/runtime/mcp-keys/`(0750 root:chatagent)。
  工單原本寫 `/root/<svc>/mcp_path_key.txt`,但 bridge 以 `chatagent` 跑而 `/root` 是 0700
  ——沒有在 Phase 0 的界線上打洞,改成搬密鑰
- dash 依方案 C 停用(只停服務,一個檔都沒刪)

> ⚠️ **兩個坑,下個窗口容易再踩**
> 1. `curl` 會在送出前把 `../` 正規化掉。測穿越要加 `--path-as-is`,否則你測到的是別的路徑。
> 2. 從 `exec_vps` 裡面探測 hands 會拿到假的 `000`——那是自我阻塞。要用
>    `systemd-run --unit=... --collect` 開獨立 cgroup 排程,不要 `setsid`
>    (`setsid` 逃得掉 session,逃不掉 service cgroup,`systemctl restart` 會連你一起殺)。

> 📌 盲測期跑到 **9/15**。8/31–9/1 17:11 標記為汙染區間。
> 糯糯 9/1 的兩項主觀回報列為 9/15 後的修正候選,**盲測期間不動**。

## 三、TICKET-H(`ticket-h/`)

StackChan 照片進相簿,做法是**對帳(reconciliation)而不是拍照當下推一筆**——
StackChan 有三條會改狀態的路徑(落地 / keep / 60 天到期),逐條掛通知等於改它的既有行為,
漏一條就會在相簿留死卡。磁碟是唯一事實來源。

已完成:落地即進暫存區、雙向同步實體檔(含 `.json`)、回填既有 12 張、空狀態文案、
相簿刪除鍵(純 icon / 最左 / 純平面,糯糯蓋章)、相簿自動刷新。

> ⚠️ **④ 還沒做,而且要等裁定**:`StackChanPanel`(App.tsx:8065)、
> `view==="stackchan"` 分支(10059)、`/api/v2/stackchan/photos*` 端點的下線。
> 工單寫明「相簿功能驗收通過後」才移除——驗收現在通過了,但糯糯還沒對「導覽入口 vs 廢棄」裁定。
> **不要自己動。**

> ⚠️ **部署層設定會掉**:`/etc/systemd/system/chatnest-next.service.d/ticket-h-photos.conf`
> 是 TICKET-H 需要的(相簿要寫 `/srv/mumu-server/photos`)。repo 備份在
> `ticket-h/deploy/`。如果整個 `.service.d/` 目錄被重建,刪除跟永久收藏會一起回 500。

---

## 這個 session 最該記住的一件事

**施工者身在被施工的系統之內。** 同一個形狀踩了三次:

1. 從 `exec_vps` 裡面探測 `exec_vps`,拿到假的 000
2. `setsid` 起的腳本被它自己下的 `systemctl restart` 殺掉(逃得掉 session,逃不掉 cgroup)
3. 查到「backend 以 root 跑」就判斷寫得進去 → 線上 `ProtectSystem=strict` 讓它唯讀,刪除回 500

共同點都是**用推論代替實測,而推論的前提正好被環境本身改寫了**。
第三次還多一層:我寫的測試把路徑指到可寫的 tmp,所以整片綠燈卻完全沒碰到真的掛載。

對策已經落地成做法,不只是心得:
- 探測要開獨立 cgroup(`systemd-run`),不要在受測物內部發起
- 沙箱行為要在**同規格的沙箱**裡實測(`ticket-h/verify_disk_writeback.py`)
- 每個修復都要有**反向對照組**:拆掉修復後測試必須變紅,不然那條測試測的不是這件事

---

## 給糯糯的部分

視覺是她的雷點,流程是**打樣 → 她蓋章 → 才上線**,而且**只改她點名的**。
她給了參考就照參考做,不要自作主張加料。吃不準先問。

這次她點名兩次(「按鍵再縮小或直接刪除新擬態突起」、「提示的框線要刪」),
兩次都只改她說的那兩點,沒有順手調別的——她的原話是「更進一步的美化是美化窗的工作」。
